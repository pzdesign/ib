$(window).scroll(function() {
/*
      if ($('li#cen').hasClass('active')){
        $('.navbar').addClass('mam'); 
      } else {
        $('.navbar').removeClass('mam');
      }

// 100 = The point you would like to fade the nav in.
  
  if ($(window).scrollTop() > 100 ){
    
    $('.navbar').addClass('show');

  } else {

    $('.navbar').removeClass('show');
 
  }; 
  */   
});
